File=Table
Product=mcp

#*************************************************

Group:
  Flavor=IRIX+6
  Qualifiers=""

Common:
   Action=setup
      proddir()
      setupenv()
      setupRequired("-f ${UPS_PROD_FLAVOR} dervish")
      setupRequired("-f ${UPS_PROD_FLAVOR} murmur_client_vx")
      setupRequired("-f ${UPS_PROD_FLAVOR} vx_tools")
      setupRequired("-f ${UPS_PROD_FLAVOR} astroda")
      setupRequired("-f ${UPS_PROD_FLAVOR} -q build vx_dsc mcp")
      pathAppend(PATH, ${UPS_PROD_DIR}/bin)
End:

#*************************************************

